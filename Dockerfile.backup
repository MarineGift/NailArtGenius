# 간단한 Dockerfile - 테스트용
FROM node:18-alpine

# 작업 디렉토리 설정
WORKDIR /app

# npm 업데이트 (호환성 문제 해결)
RUN npm install -g npm@latest

# package.json과 package-lock.json 복사
COPY package*.json ./

# 의존성 설치 (더 안전한 방법)
RUN npm install

# 모든 파일 복사
COPY . .

# Next.js 빌드
RUN npm run build

# 포트 노출
EXPOSE 3000

# 서버 시작
CMD ["npm", "start"]